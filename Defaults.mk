version = 0.0
package = speakup-tools

prefix = /usr/local
bindir = $(prefix)/bin
libdir = $(prefix)/lib
sbindir = $(prefix)/sbin
datadir = $(prefix)/share
mandir = $(datadir)/man
modprobedir = $(libdir)/modprobe.d
pkgdatadir = $(datadir)/$(package)

CHMOD = chmod
INSTALL = install
SED = sed
